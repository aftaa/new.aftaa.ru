<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8">
        <title>Welcome, aftaa!</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}">
        <link rel="stylesheet" type="text/css" href="{{ asset('bootstrap-5.3.2/css/bootstrap.css') }}">

        {% block stylesheets %}
        {% endblock %}

        <script src="{{ asset('js/jquery-3.7.1.js') }}"></script>
        <script>window.host = '{{ api_host }}';</script>
        <script src="{{ asset('js/jwt.js') }}?t={{ 'now'|date('U') }}"></script>

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        <script src="{{ asset('bootstrap-5.3.2/js/bootstrap.bundle.js') }}"></script>
    </body>
</html>
