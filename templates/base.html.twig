<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8">
        <title>Welcome, aftaa!</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}">
        {% block stylesheets %}
        {% endblock %}

        <script src="{{ asset('js/jquery-3.7.1.js') }}"></script>
        <script>window.host = '{{ api_host }}';</script>
        <script src="{{ asset('js/jwt.js') }}?t={{ 'now'|date('U') }}"></script>

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
