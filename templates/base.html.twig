<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Welcome, aftaa!</title>
    <link rel="icon" href="{{ asset('favicon.ico') }}">
    <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap-5.3.2.css') }}">
    <link rel="stylesheet" type="text/css" href="{{ asset('css/common.css') }}?t={{ 'now'|date('U') }}">
    {% block css %}{% endblock %}
    <script src="{{ asset('js/bootstrap.bundle-5.3.2.js') }}"></script>
    <script src="{{ asset('js/jquery-3.7.1.js') }}"></script>
    <script src="{{ asset('js/vue-2.7.14.js') }}"></script>
    <script src="{{ asset('js/login.js') }}?t={{ 'now'|date('U') }}"></script>
    <script src="{{ asset('js/jwt.js') }}?t={{ 'now'|date('U') }}"></script>
    <script src="{{ asset('js/spinner.js') }}?t={{ 'now'|date('U') }}"></script>
    <script>
        $(function () {
            window.host = '{{ api_host }}';
            window.modalLogin = new bootstrap.Modal('#modalLogin');
        });
        spinner();
    </script>
    {% block js %}{% endblock %}
</head>
<body>
<div class="d-flex justify-content-center" id="spinner-wrapper">
    <div class="spinner-border" role="status" id="spinner">
        <span class="visually-hidden">Загрузка...</span>
    </div>
</div>
{% include 'login/modal.html.twig' %}
{% block body %}{% endblock %}
</body>
</html>
